{
	"$schema": "https://turborepo.dev/schema.json",
	"globalEnv": [
		"NODE_ENV",
		"BETTER_AUTH_SECRET",
		"DATABASE_URL",
		"BETTER_AUTH_URL",
		"GOOGLE_CLIENT_ID",
		"GOOGLE_CLIENT_SECRET",
		"WEB_FRONTEND_URL",
		"EXPO_FRONTEND_URL",
		"SMTP_MAIL_USERNAME",
		"SMTP_MAIL_PASS",
		"SMTP_SENDER_EMAIL"
	],
	"ui": "tui",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["$TURBO_DEFAULT$", ".env*"],
			"outputs": [".next/**", "!.next/cache/**", "dist/**", "build/**"],
			"env": ["NEXT_PUBLIC_API_URL", "EXPO_PUBLIC_API_URL"]
		},
		"lint": {
			"dependsOn": ["^lint"],
			"inputs": ["$TURBO_DEFAULT$", "../../packages/eslint-config/**"],
			"outputs": [],
			"env": ["BETTER_AUTH_URL", "GOOGLE_CLIENT_ID", "GOOGLE_CLIENT_SECRET"]
		},
		"format": {
			"inputs": ["$TURBO_DEFAULT$", ".prettierrc", ".prettierignore"],
			"outputs": []
		},
		"check-types": {
			"dependsOn": ["^check-types"],
			"inputs": ["$TURBO_DEFAULT$", "../../packages/typescript-config/**"],
			"outputs": []
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
